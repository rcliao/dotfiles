{{- if (eq .chezmoi.os "darwin") -}}
#!/bin/bash

brew bundle --no-lock --file=/dev/stdin <<EOF
brew "fzf"
brew "neovim", args: ['HEAD']
brew "tmux"
brew "zsh"
brew "starship"
brew "fd"
EOF
{{ end -}}
